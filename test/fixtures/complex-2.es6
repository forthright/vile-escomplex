let s = 3

const foo = (a, b, c, d, e, f) => {
  if (a) {
    const dd = (dsd) => dsd * 3

    const x = 3

    const a = 3

    return a
  }

  let as = [ 1,2,3,4,5,6,7,8,9 ]

  if (b || c || d || e || f) {
    if (b) {
    }

    if (d) {
      if (a || c) {
      } else if (b) {
      } else {
        return 34
      }
    }

    return 2
  }
}

let as = [ 1,2,3,4,5,6,7,8,9 ]

as.forEach((p) => {
  as.forEach((f) => {
    as.forEach((z) => {
      console.log(z)
    })
  })
})
